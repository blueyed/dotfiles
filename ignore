# Global ignore file.
# Used by ripgrep (rg) and linked to for The Silver Searcher (ag).
# cache/
# media/
# asset/
tags
# build dirs, but used by e.g. luarocks as a module name!?
# build/
# node_modules

# ag/rg look at them by default (only to find out that they are binary).
*.gif
*.jpg
*.png

*.min.css

# IPython notebooks
*.ipynb

CMakeFiles/

# coverage.py
htmlcov*/

# Useful with --hidden?!
# .git/
# .tox/

# Do not exclude .travis.yml (hidden files are ignored by default).
!.travis.yml
!.circleci
